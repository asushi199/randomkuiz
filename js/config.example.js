// Contoh konfigurasi. Untuk guna sebenar:
//   1. Salin fail ini kepada js/config.js
//   2. Isikan API_URL dengan URL Web App (/exec) anda
// js/config.js diabaikan oleh git — jangan commit.
// Nota: fail ini TIDAK menimpa config.js jika config.js sudah dimuatkan dahulu.
window.EXAM_CONFIG = window.EXAM_CONFIG || {
  API_URL: "", // kosong = halaman papar amaran "tidak tersedia"
};
